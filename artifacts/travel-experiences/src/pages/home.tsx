import { useMemo, useState, type FormEvent } from 'react';
import { Link } from 'wouter';
import { ArrowRight, ArrowUpRight, CalendarDays, ChevronRight, Clock3, SlidersHorizontal, Star, Users } from 'lucide-react';
import { useCreateBooking, useCreateQuote, useGetExperience, useListExperiences, getGetExperienceQueryKey, getListExperiencesQueryKey, getListBookingsQueryKey, getGetDashboardSummaryQueryKey } from '@workspace/api-client-react';
import type { Experience, Quote, QuoteInput } from '@workspace/api-client-react';
import { ActionButton, DataTag, EmptyState, ErrorState, formatEuro, LoadingState, PageIntro, SuccessNotice } from '@/components/travel-ui';
import { useQueryClient } from '@tanstack/react-query';
import heroImage from '../../attached_assets/aperion-hero.jpg';

function ExperienceCard({ experience, onBook }: { experience: Experience; onBook: (experience: Experience) => void }) {
  return <article className="lift-card group overflow-hidden rounded-2xl border border-border bg-card transition-transform" data-testid={`card-experience-${experience.id}`}><div className="relative h-52 overflow-hidden"><img data-testid={`img-experience-${experience.id}`} src={experience.imageUrl || heroImage} alt={experience.title} className="h-full w-full object-cover transition-transform duration-500 group-hover:scale-105" /><span className="absolute left-4 top-4 rounded-full bg-[#fbf4e7]/90 px-3 py-1 font-mono-ui text-[10px] uppercase tracking-wider text-primary">{experience.category}</span><span className="absolute right-4 top-4 flex items-center gap-1 rounded-full bg-primary/85 px-2.5 py-1 text-xs text-[#fbf4e7]"><Star size={12} fill="currentColor" /> {experience.rating.toFixed(1)}</span></div><div className="p-5"><div className="flex items-start justify-between gap-3"><h3 className="font-display text-[23px] leading-tight text-primary">{experience.title}</h3><span className="whitespace-nowrap font-mono-ui text-xs text-accent">από {formatEuro(experience.priceFrom)}</span></div><div className="mt-3 flex gap-4"><DataTag>{experience.location}</DataTag><DataTag icon={Clock3}>{experience.durationHours} ώρες</DataTag></div><p className="mt-3 line-clamp-2 text-sm leading-6 text-muted-foreground">{experience.description}</p><div className="mt-4 flex items-center justify-between border-t border-border pt-4"><span className="text-xs text-muted-foreground">{experience.reviewCount} αξιολογήσεις</span><button data-testid={`button-book-experience-${experience.id}`} onClick={() => onBook(experience)} className="group/btn inline-flex items-center gap-1 text-sm font-bold text-primary hover:text-accent">Κράτηση <ArrowRight size={15} className="transition-transform group-hover/btn:translate-x-1" /></button></div></div></article>;
}

function QuotePanel({ onQuote }: { onQuote: (quote: Quote, input: QuoteInput) => void }) {
  const createQuote = useCreateQuote();
  const [form, setForm] = useState({ pickup: 'Ξενοδοχείο Ακτή, Αθήνα', stops: 'Ναύπλιο', date: '2026-09-15', startTime: '09:30', durationHours: '8', passengers: '4', vehicleType: 'van' as QuoteInput['vehicleType'] });
  const submit = (event: FormEvent) => { event.preventDefault(); const input: QuoteInput = { ...form, stops: form.stops ? form.stops.split(',').map((s) => s.trim()) : [], durationHours: Number(form.durationHours), passengers: Number(form.passengers) }; createQuote.mutate({ data: input }, { onSuccess: (quote) => onQuote(quote, input) }); };
  return <form onSubmit={submit} className="rounded-3xl bg-primary p-6 text-primary-foreground shadow-xl md:p-8" data-testid="form-quote"><div className="flex items-start justify-between gap-4"><div><p className="font-mono-ui text-[10px] uppercase tracking-[.2em] text-accent">Φτιάξε τη δική σου μέρα</p><h2 className="mt-2 font-display text-3xl leading-tight">Από πόρτα σε πόρτα,<br />χωρίς πρόγραμμα-παζλ.</h2></div><SlidersHorizontal className="text-accent" /></div><div className="mt-7 grid gap-4 sm:grid-cols-2"><label className="sm:col-span-2"><span className="field-label">Σημείο παραλαβής</span><input data-testid="input-quote-pickup" required value={form.pickup} onChange={(e) => setForm({ ...form, pickup: e.target.value })} className="field-dark" /></label><label><span className="field-label">Στάσεις</span><input data-testid="input-quote-stops" placeholder="π.χ. Αίγινα, Σούνιο" value={form.stops} onChange={(e) => setForm({ ...form, stops: e.target.value })} className="field-dark" /></label><label><span className="field-label">Ημερομηνία</span><input data-testid="input-quote-date" type="date" required value={form.date} onChange={(e) => setForm({ ...form, date: e.target.value })} className="field-dark" /></label><label><span className="field-label">Ώρα εκκίνησης</span><input data-testid="input-quote-time" type="time" required value={form.startTime} onChange={(e) => setForm({ ...form, startTime: e.target.value })} className="field-dark" /></label><label><span className="field-label">Διάρκεια</span><select data-testid="select-quote-duration" value={form.durationHours} onChange={(e) => setForm({ ...form, durationHours: e.target.value })} className="field-dark"><option value="4">4 ώρες</option><option value="6">6 ώρες</option><option value="8">8 ώρες</option><option value="10">10 ώρες</option></select></label><label><span className="field-label">Άτομα</span><select data-testid="select-quote-passengers" value={form.passengers} onChange={(e) => setForm({ ...form, passengers: e.target.value })} className="field-dark"><option value="2">2 άτομα</option><option value="4">4 άτομα</option><option value="6">6 άτομα</option><option value="8">8 άτομα</option></select></label><label><span className="field-label">Όχημα</span><select data-testid="select-quote-vehicle" value={form.vehicleType} onChange={(e) => setForm({ ...form, vehicleType: e.target.value as QuoteInput['vehicleType'] })} className="field-dark"><option value="sedan">Sedan</option><option value="van">Van</option><option value="minibus">Minibus</option><option value="bus">Bus</option></select></label></div><ActionButton type="submit" loading={createQuote.isPending} className="mt-6 w-full bg-accent text-primary hover:bg-[#f6c995]">Υπολόγισε την τιμή <ArrowRight size={16} /></ActionButton>{createQuote.isError && <p className="mt-3 text-xs text-[#f6c995]" data-testid="text-quote-error">Δεν μπορέσαμε να υπολογίσουμε τη διαδρομή. Έλεγξε τα στοιχεία σου.</p>}</form>;
}

export default function Home() {
  const { data: experiences, isLoading, isError, refetch } = useListExperiences({ query: { queryKey: getListExperiencesQueryKey(), staleTime: 300_000 } });
  const createBooking = useCreateBooking();
  const queryClient = useQueryClient();
  const [quote, setQuote] = useState<{ result: Quote; input: QuoteInput } | null>(null);
  const [selected, setSelected] = useState<Experience | null>(null);
  const [bookingDone, setBookingDone] = useState(false);
  const [category, setCategory] = useState('Όλα');
  const categories = useMemo(() => ['Όλα', ...Array.from(new Set((experiences ?? []).map((e) => e.category)))], [experiences]);
  const selectedDetails = useGetExperience(selected?.id ?? '', { query: { enabled: Boolean(selected?.id), queryKey: getGetExperienceQueryKey(selected?.id ?? ''), staleTime: 300_000 } });
  const visible = (experiences ?? []).filter((e) => category === 'Όλα' || e.category === category);
  const bookExperience = (experience: Experience) => { setSelected(experience); setBookingDone(false); };
  const submitBooking = () => { if (!selected) return; const active = selectedDetails.data ?? selected; createBooking.mutate({ data: { customerId: 'demo-customer', experienceId: active.id || undefined, title: active.title, date: '2026-09-15', passengers: 2, pickup: 'Ξενοδοχείο Ακτή, Αθήνα', total: active.priceFrom, paymentPlan: 'deposit' } }, { onSuccess: () => { setBookingDone(true); queryClient.invalidateQueries({ queryKey: getListBookingsQueryKey({ customerId: 'demo-customer' }) }); queryClient.invalidateQueries({ queryKey: getGetDashboardSummaryQueryKey() }); } }); };
  return <div className="sunset-wash"><section className="relative overflow-hidden px-5 pb-14 pt-12 md:px-10 md:pb-20 md:pt-20"><div className="mx-auto grid max-w-[1280px] items-end gap-12 lg:grid-cols-[1.02fr_.98fr]"><div className="reveal"><p className="font-mono-ui text-[10px] uppercase tracking-[.26em] text-accent">Αθήνα · Κυκλάδες · Πελοπόννησος</p><h1 data-testid="text-home-heading" className="mt-5 max-w-2xl font-display text-[54px] leading-[.94] tracking-[-.045em] text-primary sm:text-7xl lg:text-[88px]">Η μέρα σου,<br /><em className="text-[#c96c4c]">όπως πρέπει</em><br />να είναι.</h1><p className="mt-7 max-w-md text-base leading-7 text-muted-foreground">Μετακίνηση που γίνεται ανάμνηση. Διάλεξε μια έτοιμη διαδρομή ή άφησέ μας να ενώσουμε τα σημεία που θέλεις να θυμάσαι.</p><div className="mt-8 flex flex-wrap gap-3"><a data-testid="link-scroll-experiences" href="#experiences" className="inline-flex items-center gap-2 rounded-xl bg-primary px-5 py-3 text-sm font-bold text-primary-foreground hover:-translate-y-0.5">Δες εμπειρίες <ArrowRight size={16} /></a><a data-testid="link-scroll-quote" href="#custom" className="inline-flex items-center gap-2 rounded-xl border border-primary/20 bg-card/60 px-5 py-3 text-sm font-bold text-primary hover:border-accent">Φτιάξε τη δική σου</a></div></div><div className="reveal reveal-delay-2 relative min-h-[385px] overflow-hidden rounded-[32px] bg-[#d67c5a] shadow-2xl"><img src={heroImage} alt="Παραθαλάσσια διαδρομή με θέα" className="absolute inset-0 h-full w-full object-cover mix-blend-multiply opacity-75" /><div className="absolute inset-0 bg-gradient-to-t from-primary/80 via-transparent to-[#e6a07d]/20" /><div className="absolute bottom-6 left-6 right-6 flex items-end justify-between text-[#fbf4e7]"><div><p className="font-mono-ui text-[10px] uppercase tracking-[.2em] text-accent">Σημερινή έμπνευση</p><p className="mt-1 font-display text-3xl">Το Ναύπλιο σε μία μέρα</p></div><span className="grid h-12 w-12 place-items-center rounded-full border border-[#fbf4e7]/40"><ArrowUpRight size={20} /></span></div></div></div></section><section id="experiences" className="mx-auto max-w-[1280px] px-5 py-14 md:px-10 md:py-20"><PageIntro eyebrow="Επιλεγμένες διαδρομές" title="Μικρές αποδράσεις, μεγάλη αίσθηση." detail="Σχεδιασμένες από ανθρώπους που ξέρουν πού αξίζει να σταθείς — και πότε να μην κοιτάξεις το ρολόι." action={<div className="flex gap-2 overflow-x-auto">{categories.map((item) => <button key={item} data-testid={`button-category-${item}`} onClick={() => setCategory(item)} className={`whitespace-nowrap rounded-full border px-4 py-2 text-xs font-bold ${category === item ? 'border-primary bg-primary text-primary-foreground' : 'border-border bg-card text-muted-foreground hover:border-primary'}`}>{item}</button>)}</div>} />{isLoading ? <LoadingState /> : isError ? <ErrorState onRetry={() => refetch()} /> : visible.length === 0 ? <EmptyState title="Η επόμενη διαδρομή ξεκινά εδώ." detail="Δεν βρήκαμε εμπειρίες σε αυτή την κατηγορία. Δοκίμασε το «Όλα» ή σχεδίασε τη δική σου μέρα." /> : <div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{visible.map((experience, index) => <div key={experience.id} className={`reveal reveal-delay-${Math.min(index + 1, 3)}`}><ExperienceCard experience={experience} onBook={bookExperience} /></div>)}</div>}</section><section id="custom" className="border-y border-primary/10 bg-[#e7ded0] px-5 py-14 md:px-10 md:py-20"><div className="mx-auto grid max-w-[1280px] gap-10 lg:grid-cols-[.85fr_1.15fr] lg:items-center"><div><p className="font-mono-ui text-[10px] uppercase tracking-[.24em] text-accent">Custom, αλλά απλό</p><h2 className="mt-3 max-w-md font-display text-5xl leading-[.98] text-primary">Τα σημεία σου.<br /><em className="text-[#c96c4c]">Ο ρυθμός σου.</em></h2><p className="mt-5 max-w-md text-sm leading-6 text-muted-foreground">Πες μας από πού ξεκινάς, τι θέλεις να δεις και πόσο χρόνο έχεις. Η τιμή εμφανίζεται πριν αποφασίσεις.</p><div className="mt-8 grid max-w-md gap-3 sm:grid-cols-3"><div><span className="font-display text-2xl text-primary">01</span><p className="mt-1 text-xs leading-5 text-muted-foreground">Συμπληρώνεις τη διαδρομή</p></div><div><span className="font-display text-2xl text-primary">02</span><p className="mt-1 text-xs leading-5 text-muted-foreground">Βλέπεις καθαρή τιμή</p></div><div><span className="font-display text-2xl text-primary">03</span><p className="mt-1 text-xs leading-5 text-muted-foreground">Κλείνεις χωρίς άγχος</p></div></div></div><QuotePanel onQuote={(result, input) => setQuote({ result, input })} /></div></section>{quote && <div className="fixed inset-0 z-50 grid place-items-center bg-primary/50 p-5 backdrop-blur-sm"><div className="w-full max-w-lg rounded-3xl bg-card p-7 shadow-2xl" data-testid="dialog-quote-result"><div className="flex items-start justify-between"><div><p className="font-mono-ui text-[10px] uppercase tracking-[.2em] text-accent">Η εκτίμησή σου</p><h2 className="mt-2 font-display text-4xl text-primary">{formatEuro(quote.result.total)}</h2><p className="mt-1 text-sm text-muted-foreground">{quote.result.distanceKm} χλμ · {quote.result.durationHours} ώρες · {quote.input.passengers} άτομα</p></div><button data-testid="button-close-quote" onClick={() => setQuote(null)} className="text-muted-foreground hover:text-primary">×</button></div><div className="mt-6 space-y-3 border-y border-border py-5">{quote.result.breakdown.map((line) => <div key={line.label} className="flex justify-between text-sm"><span className="text-muted-foreground">{line.label}</span><span className="font-semibold">{formatEuro(line.amount)}</span></div>)}<div className="flex justify-between pt-2 text-sm font-bold text-primary"><span>Προκαταβολή</span><span>{formatEuro(quote.result.deposit)}</span></div></div><ActionButton data-testid="button-quote-book" onClick={() => { setQuote(null); setSelected({ id: '', title: `Custom διαδρομή · ${quote.input.pickup}`, location: quote.input.pickup, durationHours: quote.result.durationHours, priceFrom: quote.result.total, category: 'Custom', description: '', imageUrl: '', highlights: [], rating: 0, reviewCount: 0 }); }}>Συνέχισε στην κράτηση <ArrowRight size={16} /></ActionButton></div></div>}{selected && <div className="fixed inset-0 z-50 grid place-items-center bg-primary/50 p-5 backdrop-blur-sm"><div className="w-full max-w-md rounded-3xl bg-card p-7 shadow-2xl" data-testid="dialog-booking"><p className="font-mono-ui text-[10px] uppercase tracking-[.2em] text-accent">Τελικό βήμα</p><h2 className="mt-2 font-display text-3xl text-primary">{selected.title}</h2>{bookingDone ? <div className="mt-6"><SuccessNotice>Η κράτησή σου καταχωρήθηκε. Θα βρεις όλες τις λεπτομέρειες στην ενότητα «Οι κρατήσεις μου».</SuccessNotice><Link href="/bookings" data-testid="link-view-bookings" className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-primary">Δες τις κρατήσεις <ChevronRight size={15} /></Link></div> : <><div className="mt-5 space-y-3 rounded-2xl bg-muted/60 p-4 text-sm"><DataTag icon={CalendarDays}>15 Σεπτεμβρίου 2026 · 09:30</DataTag><DataTag icon={Users}>2 επιβάτες</DataTag><div className="flex justify-between border-t border-border pt-3"><span className="text-muted-foreground">Σύνολο</span><strong>{formatEuro(selected.priceFrom)}</strong></div></div><ActionButton data-testid="button-confirm-booking" onClick={submitBooking} loading={createBooking.isPending} className="mt-6 w-full">Επιβεβαίωσε με προκαταβολή</ActionButton>{createBooking.isError && <p className="mt-3 text-xs text-[#b24d42]" data-testid="text-booking-error">Η κράτηση δεν ολοκληρώθηκε. Προσπάθησε ξανά.</p>}<button data-testid="button-cancel-booking" onClick={() => setSelected(null)} className="mt-3 w-full py-2 text-sm text-muted-foreground hover:text-primary">Πίσω</button></>}</div></div>}</div>;
}